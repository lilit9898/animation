import styled from 'styled-components';
import {AnimatedElement} from "../AnimatedElement/AnimatedElement.styled";

export const StyledTitle = styled(AnimatedElement)`
  left: 0;
  top: 0;
  margin-bottom: 20px;
  font-size: 43px;
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  letter-spacing: -2px;
  color: rgba(34, 34, 34, 1);
  cursor: pointer;
`;
